// const debug = require('debug')('acter:services:complete');
module.exports = (context) => {
  // debug(`${context.method} ${context.path} Result:`, context.result)
  // console.log('acter:', context.acter);
  return context;
};
